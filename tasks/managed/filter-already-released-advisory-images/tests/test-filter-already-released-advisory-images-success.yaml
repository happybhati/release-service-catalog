---
apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: test-filter-already-released-advisory-images-success
spec:
  description: |
    Validates that the filter-already-released-advisory-images task successfully filters out
    images that are already present in advisories. In this test, comp1 is considered already
    released (via mock), while comp2 should remain.
  workspaces:
    - name: tests-workspace
  params:
    - name: dataDir
      type: string
      description: The location where data will be stored
  tasks:
    - name: setup
      taskSpec:
        workspaces:
          - name: data
        steps:
          - name: create-snapshot-and-rpa
            image: quay.io/konflux-ci/release-service-utils:latest
            script: |
              #!/usr/bin/env bash
              set -eux

              mkdir -p "$(workspaces.data.path)/$(context.pipelineRun.uid)/results"

              cat > "$(workspaces.data.path)/$(context.pipelineRun.uid)/test_release_plan_admission.json" <<EOF
              {
                "spec": {
                  "origin": "test-origin"
                }
              }
              EOF

              cat > "$(workspaces.data.path)/$(context.pipelineRun.uid)/test_snapshot_spec.json" <<EOF
              {
                "application": "myapp",
                "components": [
                  {
                    "name": "comp1",
                    "repository": "quay.io/redhat-prod/release",
                    "containerImage": "quay.io/redhat-prod/release@sha256:prod123",
                    "tags": ["v1.0", "latest"]
                  },
                  {
                    "name": "comp2",
                    "repository": "quay.io/redhat-prod/another",
                    "containerImage": "quay.io/redhat-prod/another@sha256:xyz",
                    "tags": ["v2.0"]
                  }
                ]
              }
              EOF
      workspaces:
        - name: data
          workspace: tests-workspace

    - name: run-task
      runAfter:
        - setup
      taskRef:
        name: filter-already-released-advisory-images
      params:
        - name: snapshotPath
          value: "$(context.pipelineRun.uid)/test_snapshot_spec.json"
        - name: releasePlanAdmissionPath
          value: "$(context.pipelineRun.uid)/test_release_plan_admission.json"
        - name: resultsDirPath
          value: "$(context.pipelineRun.uid)/results"
        - name: dataDir
          value: "$(params.dataDir)"
        - name: taskGitUrl
          value: "http://localhost"
        - name: taskGitRevision
          value: "main"
        - name: pipelineRunUid
          value: $(context.pipelineRun.uid)
      workspaces:
        - name: data
          workspace: tests-workspace

    - name: check-result
      runAfter:
        - run-task
      taskSpec:
        params:
          - name: result
            type: string
          - name: filtered_snapshot
            type: string
        steps:
          - name: assert-filtered
            image: quay.io/konflux-ci/release-service-utils:latest
            script: |
              #!/usr/bin/env bash
              set -eux

              if [[ "$(params.result)" != "Success" ]]; then
                echo "Task failed: $(params.result)"
                exit 1
              fi

              echo "Filtered Snapshot:"
              echo '$(params.filtered_snapshot)' | jq .

              # Verify comp1 is filtered out
              if echo '$(params.filtered_snapshot)' | jq -e '.components[] | select(.name == "comp1")' > /dev/null; then
                echo "Error: comp1 should have been filtered out"
                exit 1
              fi

              # Verify comp2 remains
              if ! echo '$(params.filtered_snapshot)' | jq -e '.components[] | select(.name == "comp2")' > /dev/null; then
                echo "Error: comp2 should still be in the filtered snapshot"
                exit 1
              fi

              # Verify only one component remains
              count=$(echo '$(params.filtered_snapshot)' | jq '.components | length')
              if [ "$count" -ne 1 ]; then
                echo "Expected 1 component after filtering, found $count"
                exit 1
              fi
      params:
        - name: result
          value: "$(tasks.run-task.results.result)"
        - name: filtered_snapshot
          value: "$(tasks.run-task.results.filtered_snapshot)" 